import Vue from 'vue'
import ElementUI from 'element-ui'
// cdn 引入 antd

// Vue.config.warnHandler = function (msg, vm, trace) {
//   if (!msg.includes('pagination') && !msg.includes('pageSizeOptions')) {
//     console.error(msg, trace)
//   }
// }

Vue.use(ElementUI)
